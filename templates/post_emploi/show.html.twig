{% extends 'base.html.twig' %}

{% block title %}PostEmploi{% endblock %}

{% block body %}
    <h1>PostEmploi</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ post_emploi.id }}</td>
            </tr>
            <tr>
                <th>Lettre</th>
                <td>{{ post_emploi.lettre }}</td>
            </tr>
            <tr>
                <th>PostAt</th>
                <td>{{ post_emploi.postAt ? post_emploi.postAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

{#    <a href="{{ path('post_emploi_index') }}">back to list</a>#}

{#    <a href="{{ path('post_emploi_edit', {'id': post_emploi.id}) }}">edit</a>#}

{#    {{ include('post_emploi/_delete_form.html.twig') }}#}
{% endblock %}
