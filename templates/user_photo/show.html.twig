{% extends 'base.html.twig' %}

{% block title %}UserPhoto{% endblock %}

{% block body %}
    <h1>UserPhoto</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_photo.id }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ user_photo.image }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ user_photo.updatedAt ? user_photo.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_photo_index') }}">back to list</a>

    <a href="{{ path('user_photo_edit', {'id': user_photo.id}) }}">edit</a>

    {{ include('user_photo/_delete_form.html.twig') }}
{% endblock %}
