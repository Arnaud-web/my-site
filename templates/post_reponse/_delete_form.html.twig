<form method="post" action="{{ path('post_reponse_delete', {'id': post_reponse.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ post_reponse.id) }}">
    <button class="btn">Delete</button>
</form>
